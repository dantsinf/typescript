import React from 'react';
//Corrige

const Validacao = ({ p, a }) => {
  return (
    <div>
      {/*<h3>{p !== 0 && a !== 0 ? 'Informe um valor válido' : ''}</h3>*/}
    </div>
  );
};

export default Validacao;
